# DECISIONS

決定ログ。追記のみ。新しいものを下に足す。1件の型:

```
## YYYY-MM-DD 決定の見出し
- 決定:
- 理由:
- 捨てた案:
- 影響: （SPEC のどこを変えたか）
```

## {{date}} `.ai/` をこのリポジトリのSSOTにする
- 決定: 開発の真実は `.ai/SPEC.md` に置き、決定は本ファイルに追記する。`.ai/` は gitignore
- 理由: チャット履歴は消え、使うAIも変わる。ファイルなら次のセッション・別のAIが同じ地点から再開できる
- 捨てた案: docs/ にコミットする（履歴が汚れる・下書き段階で共有される）／チャット履歴に頼る（再現不能）
- 影響: `CLAUDE.md` と `AGENTS.md` を `.ai/SPEC.md` へのポインタにした
